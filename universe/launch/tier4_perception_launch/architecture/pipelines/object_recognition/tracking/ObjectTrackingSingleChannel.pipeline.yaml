name: ObjectTrackingSingleChannel.pipeline

instances:
  - instance: multi_object_tracker
    entity: MultiObjectTracker.module

external_interfaces:
  input:
    - name: detected_objects
  output:
    - name: objects
  parameter: []

connections:
  - from: input.detected_objects
    to: multi_object_tracker.input.detection01/objects
  - from: multi_object_tracker.output.objects
    to: output.objects

